<h2>商品詳細</h2>
<%= attachment_image_tag @item, :image, format: 'jpeg', size: "100x100", fallback: "nagano.jpg" %>

商品名
商品説明
ジャンル
税込価格
販売ステータス

<%= @item.name %>
<%= @item.description %>
<%= @item.genre.name %>
<%= @item.price %>

  <% if  @item.sale_status == true %>
    販売中
  <%else%>
    販売停止中
  <% end %>

<%= link_to edit_admin_item_path(@item) do %>
  編集する <!--ここボタンにしてください-->
<% end %>