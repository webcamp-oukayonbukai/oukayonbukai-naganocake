<%= form_with model: [:admin, genre], local: true do |f| %>
  <%= f.text_field :name %>
  <% if genre.id.nil? %>
    <%= f.submit "新規登録" %>
  <%else%>
    <%= f.submit "変更を保存" %>
  <% end %>
<% end %>