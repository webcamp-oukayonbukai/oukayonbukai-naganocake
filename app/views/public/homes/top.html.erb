<div class="container">
  <div class="row">
    <div class="col-md-12">

      <div class="theTarget">
        <div class="main-visual">
          <div class="main-visual-content">
            <div class="shop-name">
              <p class="text-light">NAGANO CAKE</p>
            </div>
              <p class="text-white">ようこそ、ながのCAKEへ！</p>
              <p class="text-white">このサイトは、ケーキ販売のECサイトになります。</p>
              <p class="text-white">会員でない方も商品の閲覧はできますが、</p>
              <p class="text-white">購入には会員登録が必要になります。</p>
          </div>
        </div>
          <div class="image1"></div>
          <div class="image2"></div>
          <div class="image3"></div>
          <div class="image4"></div>
      </div>

      <div class="genre-button text-center mt-5">
        ▶︎　▶︎
        <% @genres.each do |genre| %>
          <%= link_to genre.name, items_path(id: genre), class: 'btn btn-sm' %>
        <% end %>
          ◀︎　◀︎
      </div>

      <div class="new-items">
        <h3>新着商品</h3>
        <% @items.each do |item| %>
        <table class= "d-inline-block">
          <tr>
            <td>
              <% if admin_signed_in? %>
                <%= link_to admin_item_path(item) do %>
                  <%= attachment_image_tag(item, :image, :fill, 270, 150) %>
                <% end %>
              <% else %>
                <%= link_to item_path(item) do %>
                  <%= attachment_image_tag(item, :image, :fill, 270, 150) %>
                <% end %>
              <% end %>
            </td>
          </tr>
          <tr>
            <td><%= item.name %></td>
          </tr>
          <tr>
            <td>¥<%= item.price.to_s(:delimited) %></td>
          </tr>
        </table>
        <% end %>
        <p class="text-md-right">
          <%= link_to "全ての商品を見る>", items_path %>
        </p>
      </div>

    </div>
  </div>
</div>

